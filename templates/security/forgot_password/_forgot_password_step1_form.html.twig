{{ form_start(form, {'method': 'POST', 'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}
    {% include 'form/_input.html.twig' with {'formElm': form.email} %}
<div class="mt-5">
    <button type="submit"  class="btn btn-outline-info w-100" formnovalidate>Reset my password</button>
</div>
{{ form_rest(form) }}
{{ form_end(form) }}